#ifndef STACKA_H
#define STACKA_H

#include "amll.h"

// Fungsi Stack (Menggunakan linked list)
void push(Node **top, int value);
int pop(Node **top);
void convertDecimalToBinary(int decimal);

#endif
